#include "linkedlist.h"
/*
int main(){
    // Starts with a empty list
    struct Orders *head = NULL;

    // Adding orders
    add_linkedlist(&head, 5);
    add_linkedlist(&head, 2);

    // printing linked list
    print_linkedlist(head);
    return 0;
}
*/